ktor { deployment { port = 8102; port = ${?AUDIT_PORT} } application { modules = [ com.ecommerce.audit.ApplicationKt.module ] } }
audit { database { url = ${?AUDIT_DATABASE_URL}; username = ${?DATABASE_USERNAME}; password = ${?DATABASE_PASSWORD}; maximumPoolSize = 20; connectionTimeoutMillis = 2000 } jwt { issuer = "commerce-identity"; audience = "commerce-api"; keys = ${?JWT_KEYS} } kafka { bootstrapServers = ${?KAFKA_BOOTSTRAP_SERVERS}; groupId = "audit-service-v1"; topics = "user.events.v1,seller.events.v1,cms.events.v1,feature-flags.events.v1,admin.events.v1,catalog.events.v1,pricing.events.v1,promotion.events.v1,order.events.v1,payment.events.v1,refund.events.v1,inventory.events.v1,shipping.events.v1,review.events.v1" } }
