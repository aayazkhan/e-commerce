# Production Terraform/OpenTofu environment

Production provisioning is approval-gated. Require separate state, MFA/workload identity, drift detection, encrypted remote state, backup/PITR configuration, certificate monitoring, network policy, and an operator rollback plan. Provider-specific implementation is pending cloud selection.
