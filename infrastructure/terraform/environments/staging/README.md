# Staging Terraform/OpenTofu environment

Staging must mirror production topology classes with smaller capacity and synthetic/masked data. Require a reviewed plan, restore-capable backups, external secrets, TLS, and observability before applying.
