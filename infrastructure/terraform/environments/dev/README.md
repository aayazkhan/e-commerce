# Dev Terraform/OpenTofu environment

State backend, provider, region, network CIDRs, and credentials are intentionally unset. Add them through the selected platform’s CI environment; never commit a state file or secret.
